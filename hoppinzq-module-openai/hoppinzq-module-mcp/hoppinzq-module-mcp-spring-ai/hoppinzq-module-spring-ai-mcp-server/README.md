### 基于stdio的最基本的MCP Server

### 如何使用

#### 1、打包该项目，将jar包放在随便一个位置（如D盘里）。

#### 2、在Cherry Studio里选择MCP设置，然后添加一个MCP服务器。传输方式选择`stdio`，命令填写`java`，参数是`-jar D:\hoppinzq-module-spring-ai-mcp-server-0.0.1-SNAPSHOT.jar`（注意路径要改成你jar包所在的路径）。

#### 3、开始对话。示例提示词：

- AI，你知道哪些网站
- AI，打开张祺的网站

### 这只是一个简单的示例
